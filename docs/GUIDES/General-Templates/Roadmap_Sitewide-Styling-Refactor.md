
# Stage 1

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Overlays/Accordions: migrate all site dialogs/popovers/accordions to Radix primitives + standard overlay/motion (nav flyouts if needed, chat, engravings, shotguns model dialogs, heritage event dialog, FAQs).

---

# Stage 2

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Buttons/Inputs: normalize Button/Input/Textarea usage and styles; replace one-off CTA/button/input classes with src/components/ui primitives; remove ad‑hoc button utilities where safe.

---

# Stage 3

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Typography: standardize non‑hero headings/body text on Heading/Text primitives; keep bespoke hero typography; align section heading patterns.

---

# Stage 4

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Sections/Containers: normalize layout wrappers to Section/Container primitives; remove custom padding/radius/shadow one‑offs as they are replaced.

---

# Stage 5

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Rich Text: consolidate CMS content to Portable Text; keep SafeHtml only for legacy HTML blocks; align prose styling and reduce mixed renderers.

---

# Stage 6

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Tokens Cleanup: align/remove legacy tokens.ts usage; replace raw hex colors with CSS vars; enforce premium radius/shadow scale.

---

# Stage 7

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Icon Consistency: replace any non‑lucide icons in site UI; ensure consistent sizing/stroke.

---

# Stage 8

**Using the `docs/ui-style-contract.md` guide and any relevent files listed within it, go ahead and implement the following:**

* Final Polish: sweep for remaining one‑offs, update stray custom shadows/radii/spacing, and refresh the style contract if needed.