@@ -100,1 +100,1 @@
-        archetype_scores: normAgg ? (normAgg as any) : filtered[i].archetype_scores,
+        archetype_scores: normAgg ? (normAgg as unknown) : filtered[i].archetype_scores,
@@ -183,1 +183,1 @@
-                const snappedRaw = (r as any).archetype_snapped;
+                const snappedRaw = (r as unknown).archetype_snapped;
@@ -188,1 +188,1 @@
-                const norm = normalize(scores as any);
+                const norm = normalize(scores as unknown);
@@ -197,1 +197,1 @@
-                  const { winner, runner, margin } = computeWinnerAndRunner(norm as any);
+                  const { winner, runner, margin } = computeWinnerAndRunner(norm as unknown);
@@ -206,1 +206,1 @@
-                  typeof (r as any).archetype_confidence_margin === "number"
+                  typeof (r as unknown).archetype_confidence_margin === "number"
@@ -207,1 +207,1 @@
-                    ? (r as any).archetype_confidence_margin
+                    ? (r as unknown).archetype_confidence_margin
@@ -220,1 +220,1 @@
-                  const prevNorm = normalize(prevScores as any);
+                  const prevNorm = normalize(prevScores as unknown);
@@ -222,1 +222,1 @@
-                    prevWinnerText = computeWinnerAndRunner(prevNorm as any).winner.k;
+                    prevWinnerText = computeWinnerAndRunner(prevNorm as unknown).winner.k;
