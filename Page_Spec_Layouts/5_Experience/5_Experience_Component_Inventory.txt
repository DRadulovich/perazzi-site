files["5_Experience_Component_Inventory.txt"] = textwrap.dedent("""\
# Experience Perazzi – Mini Component Inventory

By Page Section
- `ExperienceHero` (reuse `HeroBanner` with experience media)
- `ExperiencePicker` (new) — 3 pathway cards
- `VisitFactory` (new composite) — address/map/itinerary + CTA
- `BookFitting` (reuse booking options with copy deltas)
- `DemoProgram` (new) — upcoming events + request CTA + etiquette
- `MosaicGallery` (new) — photo/video thumbs + Radix Dialog lightbox
- `FAQList` (new/reuse hybrid) — Q/A + optional JSON‑LD
- `CTASection` (reuse) — primary: Request a Visit; secondary: Begin Your Fitting

By Component Type
- **Hero:** `ExperienceHero`
- **Cards & Grids:** `ExperiencePicker`, `BookingOptions` (reuse)
- **Composite:** `VisitFactory` (map + itinerary), `DemoProgram` (list + request)
- **Galleries:** `MosaicGallery` (lightbox)
- **Text:** `FAQList`, “What to Expect” notes
- **CTA:** `CTASection`
"""])